# Point this to your FastAPI backend base (public IP or domain)
# Example if your backend is also on EC2 with Nginx:
# BACKEND_API_BASE=http://YOUR_BACKEND_HOST
BACKEND_API_BASE=http://YOUR_BACKEND_HOST

# Simple shared secret so randoms can’t spam your ingest endpoints
INGEST_TOKEN=change_this_to_long_random

# Tag events by a default area (used by the Flask web honeypot)
HONEYPOT_AREA=Palasia
